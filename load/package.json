{
  "name": "load",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "build:hooks": "tsc -p tsconfig.json",
    "test:load": "npm run build:hooks && artillery run scenarios/auth-load.yml -o reports/auth-load.json && npx artillery report reports/auth-load.json reports/auth-load.html"
  },
  "devDependencies": {
    "artillery": "^2.0.23",
    "typescript": "^5.x",
    "@types/node": "^20.x"
  }
}
