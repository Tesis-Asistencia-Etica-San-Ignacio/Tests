{
  "name": "load",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preload": "tsc -p tsconfig.json",
    "test:load": "npm run preload && artillery run scenarios/auth-load.yml --config artillery.config.js -o reports/auth-load.json && artillery report -o reports/auth-load.html reports/auth-load.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "artillery": "^2.0.21"
  }
}
